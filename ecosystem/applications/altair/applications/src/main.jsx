import React from "react";
import ReactDOM from "react-dom/client";
import AltairApp from "./app/AltairApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AltairApp />
  </React.StrictMode>
);
